# Gilbert Kevin Jimmy Kwizera

Personal portfolio for Gilbert Kevin Jimmy Kwizera. Content is drawn from the public record at [kwizeraz.org](https://kwizeraz.org/). The interface is original.

## Scripts

- `npm install`
- `npm run dev` — local development
- `npm run build` — production build
- `npm run preview` — preview the build
- `npm run lint` — ESLint
